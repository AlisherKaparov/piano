import 'package:flutter/material.dart';

class Axempl extends StatelessWidget {
  const Axempl({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
